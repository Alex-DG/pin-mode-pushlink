<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/darker_gray"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/title_screen_pinning_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="97dp"
        android:textColor="@android:color/black"
        android:text="Screen Pinning with Android LollipopXX"
        android:textSize="20dp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/enable_screen_pinning_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/title_screen_pinning_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="Enable" />

    <Button
        android:id="@+id/disable_screen_pinning_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/enable_screen_pinning_button"
        android:layout_below="@+id/enable_screen_pinning_button"
        android:text="Disable" />

    <TextView
        android:id="@+id/screen_pinning_description_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="100dp"
        android:textStyle="bold"
        android:textSize="15dp"
        android:text="@string/screen_pinning_not_active_text" />

</RelativeLayout>
